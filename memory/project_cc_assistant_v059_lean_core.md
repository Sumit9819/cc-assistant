---
name: cc-assistant-v059-lean-core
description: "v0.59.0 SHIPPED — capabilities manifest in whoami (continuity guarantee), token pass (probe diff, section filter, envelope strip), transport retry, info-gain gate, tests in repo"
metadata: 
  node_type: memory
  type: project
  originSessionId: 598fc17d-01d9-403a-b093-67c7c45a8e9b
  modified: 2026-07-31T04:17:47.037Z
---

v0.59.0 "Lean Core + Never Forget" (built 2026-07-30, zip 103 files; deployed sites run 0.58.0 — whoami drift warning will now nudge correctly after /mcp reconnect):

- **Capabilities manifest** (includes/class-capabilities.php, embedded in whoami.session_recap.capabilities): versioned feature+workflow index generated by the plugin — a new chat learns every capability in call one. THE continuity mechanism; update the registry when adding features.
- **Token pass**: render_probe `diff=true` (24h transient baseline per post; unchanged facets collapse to counts) — use for the AFTER probe; widget_schema `section` arg (full button schema 61KB -> few hundred bytes; unfiltered responses list `sections`); bridge strips the legacy {site,data} envelope in format_tool_result (cc_unwrap_envelope) — one shape everywhere.
- **Fixed**: whoami drift check read plugin_version above the data envelope (would false-warn on every site — 3rd envelope bug; the unwrap kills the class).
- **Transport retry**: wp_rest_call retries GETs twice w/ backoff on connection-level errors (SiteGround sync drops); never POSTs (double-queue risk).
- **Info-gain gate** (warn-only, draft_create_post): zero non-commodity signals ($price, <table, .gov/.edu link, credential regex MD|DO|FNP-C|APRN|PA-C|RN|DDS|DVM|PharmD, blockquote) -> COMMODITY RISK warning; otherwise reports info_gain_signals. Commodity doctrine: classify pages INVEST / CITE-PLAY / BRIDGE / STOP.
- **Tests in repo**: wp-content/plugins/cc-assistant/tests/ (warehouse 27, outcome-DiD 16, attention 34, schema 17 checks) + run.sh; 'tests' dir zip-excluded by build-zip.py. Run before every release: `bash tests/run.sh`.
- CLAUDE.md gained the weekly harvest ritual (striking-distance / CTR-leak / momentum sweeps -> top 3 actions; monthly outcome_report).

v0.60.0 "AEO Command Center" SHIPPED same day (zip 104 files, 140 tools): `commodity_audit` (bridge, bin/warehouse.php cc_commodity_classify PURE + cc_wh_tool_commodity_audit) — INVEST/BRIDGE/CITE_PLAY/STOP router; absorption = CTR < 25% of positional expectation (cc_expected_ctr curve) at 200+ imp; joins new POST /commodity/signals (includes/class-rest-aeo.php: url_to_postid + intent family + info_gain_signals, now PUBLIC on CC_Assistant_REST_API). `aeo_snapshot` — aeo_snapshots table in warehouse DB; absorbed-query census SQL + llm_crawls(7d) + gsc/ai-overview; vs_previous deltas. Capabilities manifest updated (commodity_router, aeo_tracking). tests/commodity-test.php (17 checks) — caught a real bug pre-ship: cc_unwrap_envelope lived in mcp-server.php but warehouse tools need it standalone; MOVED to warehouse.php (dependency rule: warehouse.php must be self-sufficient for the harness).

ROADMAP NEXT: v0.61 "Fleet + Links" = warehouse-driven internal-link engine (query-data-driven, beats text-matching Link Whisper) + fleet ops (one command -> all sites) + cross-site playbook mining. Then Review Deck (UX release). Deployed sites run 0.58.0 — one zip now carries 0.54-0.60; deploy + /mcp reconnect to activate everything. First real uses queued: commodity_audit on erofirving (the 91k-impression dehydration cluster = textbook CITE_PLAY), calendar re-screen through commodity lens before Aug drafting.
