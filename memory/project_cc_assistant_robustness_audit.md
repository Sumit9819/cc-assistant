---
name: project_cc_assistant_robustness_audit
description: "3-pillar source-level audit of cc-assistant plugin (continuity, page-build quality, systemic robustness) with P0/P1/P2 fix plan — done 2026-06-10"
metadata: 
  node_type: memory
  type: project
  originSessionId: cf6b72ff-7286-4f35-bac6-cc72e684abbc
---

Source-level audit of cc-assistant (v0.35.5) run 2026-06-10 after the WR service-hub page shipped bad (51/100, no images, 22 lorem-ipsum ungrouped cards) and a fresh chat failed to resume prior work. Three pillars:

**Pillar 1 — Continuity (user's #1 pain: "new chat is a loss").** Plugin persists event-history + free-text notes, NOT task-state. whoami truncates: notes_tail last 500 chars, decisions_tail 800, rules 1500, activity log pruned at 48h (class-rest-api.php:884-886, class-activity-log.php:132). No structured current-job record. FIX: new `cc_working_state` table {active_task, target_post_ids, step_checklist, decisions, constraints, open_loops, current_plan}, returned IN FULL by whoami; tools set_working_state/update_working_state; AUTO-CAPTURE on queue/apply/reject (class-pending-changes.php:8,414) so it stays current when AI forgets. Auto-layer only augments, never overwrites intent fields. New class includes/class-working-state.php; table in class-activator.php:404.

**Pillar 2 — Page-build quality.** (a) `replace_section_content` (class-sections.php:138) runs ZERO lint = lorem-ipsum vector. (b) NO absolute image gate — all image checks pass image-free pages (class-pre-publish.php:766, class-page-audit.php:724); page_completeness_score critical_missing is advisory + mirror-relative, never called by build path. (c) No IA/grouping concept anywhere (22 flat cards). (d) only macro build_service_page CLONES a mirror; no build-from-spec template. FIX: lint sections path; absolute image-presence gate wired into apply; broaden placeholder lint (only matches literal "Lorem ipsum" at class-pre-publish.php:1387) + duplicate-card detector; flat-list IA check; `build_section_from_template` macro with required image+group slots + auto-grouping; media-library image-sourcing tool.

**Pillar 3 — Systemic robustness.** P0: redirect dedup `find_existing_redirect_for_source()` (class-rest-api.php:5870) DROPS status filter → trashed RM rows cause ghost 409 (THIS was today's Spanish-redirect block — NOT SiteGround cache as I told user). No delete/untrash/edit-redirect tools. tmp/mojibake_fix/dump_posts.php web-reachable, no ABSPATH guard → delete tmp/. P1: readme Stable tag 0.35.0 ≠ code 0.35.5, no auto-update so fixes never reach sites; MCP serverInfo.version hardcoded 0.1.0; post-apply audit sslverify=true dies on .local; MCP errors unstructured + unlogged. P2: snapshot double-writes _elementor_data; uninstall.php drop-shared-tables hazard + stale lists; cache flush misses RM redirect cache. SOUND (don't touch): apply idempotency/race-safety, override_lint uniformity (except sections), permissions, self/reverse-loop guards.

Recommended sequence: Release A = P0 safety (redirect lifecycle + section lint + delete tmp/ + version sync, mostly surgical); Release B = continuity; Release C = page quality (small gates first, template macro later). Deploy via Python zipfile (forward slashes) per [[reference_zip_packaging_gotcha]]. Relates to [[project_cc_assistant_seo_alignment]] and [[feedback_plugin_performance]].

**FEATURE-REFINEMENT AUDIT (4 more tracks, 2026-06-10):** (4) TOOL SURFACE — 98 tools too many; P0 naming traps: draft_update_post_meta actually edits post FIELDS not meta (rename→draft_update_post_field), audit_post_links vs links_audit_post swapped arg names, 3 queue-prefixes (draft_/propose_/none); ~25 overlapping audit/score tools to consolidate; get_post returns 80-116KB (make slim default); add default keys + trim bloated descriptions + paginate list tools (bin/mcp-server.php). (5) ADMIN UX — P0: pending-inbox uses THIN diff renderer (CC_Assistant_Diff_Render::render_human_diff via class-rest-pending.php:129) that returns "(no preview available)" for ~8 change types INCLUDING the new delete_redirect/untrash_redirect I shipped + post_content_update + widget add/remove/container/accordion; a RICHER inline renderer exists but is dead code (shadowed) in pending.php:585-881 — port it into class-diff-render.php + add redirect cards w/ destructive warning; dashboard cards otherwise advisor-mode compliant; MISSING human-side "what is AI doing" activity view (data exists in cc_activity_log); onboarding step-2 can't self-detect. (6) ANALYTICS ACCURACY — 3 cross-cutting root causes: (a) NO language segmentation despite solid CC_Assistant_Multilingual (missing_mentions/content_gaps flag ES as gaps; byline regex fails accented names), (b) hardcoded medical/US/TX incl REAL BUG class-similarity.php:351 hardcodes irving/tx/texas stripped from EVERY site's corpus skewing all clustering, (c) pre-AIO CTR: two disagreeing 2024 curves (gsc.php:1959 pos1=.317 vs seo-tools.php:669 pos1=.395); classify_cluster size>=3→always "redundant/redirect" = destructive advice on legit hub-spoke. (7) STRATEGIC GAPS + FE PERF — FE discipline STRONG (only violation: class-schema-cleanup.php:307-329 logs activity-log INSERT on wp_head render path + blank-log arg bug line 322); ZERO features for reviews(#1 lever, plugin only STRIPS review schema), GBP/local-pack, citation/NAP tracking, forward image-sourcing (list_image_placeholders is import-only), AEO beyond FAQ; class-llm-tracker.php is good (crawl-input, extend to citation-outcome).

ROADMAP (4 tiers): T0 correctness/safety (diff-render consolidation + redirect cards, similarity hardcoded-token bug, schema-cleanup FE write+blank-log, get_post slim default, rename draft_update_post_meta); T1 continuity cc_working_state + analytics trust (lang seg + industry-source + AIO CTR + confidence flags); T2 page-quality (image gate+IA+template macro) + tool consolidation; T3 net-new features (reviews/GBP/image-sourcing/citations/AEO) + admin activity view. RELEASE A GAP: shipped redirect tools WITHOUT their diff renderer — fix in T0.

**PAGE-QUALITY ENGINE PART 3 — win_audit SHIPPED 2026-06-11 (v0.41.0, zip 76 files). ENGINE COMPLETE (gate+builder+win-audit).** Research first (2 agents): (a) verified 2026 evidence — fanout coverage +161% AI-citation (Surfer 10k + Ahrefs 863k), info-gain +25-40% & +115% at pos-5 (Princeton GEO arXiv 2311.09735), freshness first-order (cited content 25.7% fresher, <3mo ~3x), AIO citations from top-10 fell 76%→38% in 6mo (score ranking + citation SEPARATELY), REFUTED: schema-as-citation-lever (Ahrefs n=1885, −4.6%), word-count, keyword density (now negative), date-bumping, FAQ-schema; (b) plugin inventory — REUSE (HCS/check_post/page_audit/gate/GSC page_queries/Similarity/playbook/isolate_main_content+build_audit_dom), CAUTION (2 divergent pre-AIO CTR curves gsc:1959 vs seo-tools:669 STILL unfixed; HCS AI-tells no language gate; freshness bucket penalizes outside-plugin edits; eeat hard-fails byline-banned sites; link-audit is_broken uses old bot UA missed v0.35.3 migration; analyze_post_structure near-blind on Elementor), MISSING (external fetcher, HTML-string fingerprinter, text-input tokenizer, aggregator) — all 4 missing pieces now built. NEW includes/class-win-audit.php: CC_Assistant_Win_Audit::audit({post_id, query?, competitor_urls?[2-5 from model's web search — no SERP API], days}) → {score, verdict winning/competitive/behind/losing, dimensions{10}, actions[] priority=weight×gap, competitors[], weights, notes}. Weights in cc_assistant_win_audit_weights option (drift-proof): info_gain 18/intent_format 15/fanout 15/freshness 10/extractability 10/eeat 8/effort_media 8/title_ctr 8/internal_support 6/schema_hygiene 2. Skipped dims excluded from weighted total. SSRF-guarded fetch (no IPs/local/own-host, 2MB, 6h transient cache). NEW cc_assistant_policy_no_bylines option → eeat reroutes to citation-only (erofwhiterock!). NEW Similarity::terms_for_text() public. REST POST /posts/{id}/win-audit + MCP tool win_audit. CAVEATS: internal_support guesses table name wp_cc_internal_links (skips gracefully if wrong — verify); not live-tested; CTR-curve unification + remaining CAUTION fixes still TODO; workflow = model web-searches query → passes URLs → executes actions → re-runs.

**PAGE-QUALITY ENGINE PART 2 — build_page_from_spec SHIPPED 2026-06-11 (v0.40.0, zip 75 files):** NEW includes/class-build-from-spec.php (CC_Assistant_Build_From_Spec::build) + POST /draft/build-from-spec + MCP tool build_page_from_spec. Composes a COMPLETE new page from typed section templates in ONE call: hero (H1+bg image+overlay+buttons), text_image (H2+html+image w/ REQUIRED alt, left/right), card_grid (icon-box CSS grid, >10 cards REQUIRE per-card 'group' → titled H3 sub-grids), faq (classic accordion widget w/ tabs repeater, title_html_tag h3, AEO), cta_band. ENFORCED at build: ≥1 image (override_images), exactly one H1, full per-widget lint + detect_placeholders + detect_duplicate_blocks BEFORE wp_insert_post, validate_tree. Styles sampled from style_mirror_post_id via add_container('' root, ..., $style_mirror) brand-defaults pass — global, no tenant hardcoding. Widths follow 1300 root boxed / 850 inner rules (_element_custom_width + _flex_align_self center). Persistence mirrors build_service_page (draft + race-stamp + _elementor_data wp_slash + edit_mode builder + template type from mirror + set_seo_field [made PUBLIC] + schema-generator + ONE publish_draft pending; v0.39 gate re-checks at apply). dry_run validates w/o side effects. NOT live-tested (needs deploy + MCP restart). NEXT: part 3 win-audit (page vs 2026 criteria + top-3 competitors → prioritized beat-them list).

**PAGE-QUALITY ENGINE PART 1 — enforced gate SHIPPED 2026-06-10 (v0.39.0):** First of the 3-part "win in 2026" page engine (1=enforced gate DONE, 2=build-from-spec template macro TODO, 3=win-audit vs competitors+2026 TODO). class-pre-publish.php: evaluate_publish_gate($post_id) walks whole tree → BLOCKING (placeholder/lorem text via broadened placeholder_patterns(), duplicate_card_text = 3+ siblings identical copy via detect_duplicate_blocks) + WARNINGS (no in-body images, flat-list >10 same-type cards, thin <300 words). Enforced in class-apply.php apply_publish_draft (refuses publish on blocking, error 'publish_gate_blocked', overridable via _cc_publish_gate_override postmeta or cc_assistant_publish_gate_blocking filter). duplicate_card_text added to QUEUE-time hard lints (rest-api lint_per_widget_payloads + sections lint_widget_updates) so unfilled-template cards refused at container_add/replace_section_content before draft. NEW page_quality_gate MCP tool + GET /posts/{id}/quality-gate (preview, read-only, for self-check + refresh). Would have caught the original 22-lorem-ipsum page (double-blocked: placeholder + dup). All 6 files php -l clean, zip v0.39.0 (74 files). NOT live-tested (plugintesting offline); MCP restart needed for page_quality_gate tool. NEXT: part 2 build-from-spec macro, part 3 win-audit.

**GBP LIVE STATUS (2026-06-10):** v0.38.1 deployed to erofwhiterock + GBP OAuth CONNECTED successfully. gbp_locations(refresh) live-tested → returns Google 429 "Quota exceeded ... limit 'Requests per minute' of service mybusinessaccountmanagement.googleapis.com" (project_number 518134550279). ROOT CAUSE = Google quota "Requests per minute" = 0 (default on My Business APIs until the Business Profile APIs access/quota request form is approved). PLUGIN CODE IS CORRECT END-TO-END (429 proves OAuth + rawurlencode-fix + api_get error-surfacing all work). NO code action needed — blocked purely on user's Google quota approval (request per-minute increase on Account Management + Business Information + Performance APIs + submit GBP access form, ~days). Once approved, gbp_locations/gbp_performance will work with zero changes. MCP tools confirmed available this session for all sites.

**QA BUG-HUNT + FIXES SHIPPED 2026-06-10 (v0.38.1):** 4 adversarial review agents over all v0.36-0.38 changes. Redirect lifecycle + de-hardcoding = CLEAN (confirmed change_status('active') correct — RankMath stores status as STRINGS not ints despite stale docblock). Bugs found+fixed: (P0) get_post auto-slim was DEAD CODE — route registers 'slim' default=false so get_param('slim') never null → $slim_explicit always true → guard unreachable; fixed via array_key_exists on get_query_params/get_json_params. (P0) GBP rawurlencode($account_name/$location_name) into path broke ALL location+performance calls (accounts/123→accounts%2F123, gRPC path template rejects); fixed to pass raw resource names. (P1) GBP api_get swallowed token-refresh WP_Error on 401; now returns $refresh. (P2) redirect _diag now emits status_skipped to mirror live filter; removed em dash from get_post notice; clamped GBP perf lookback to 537d. All php -l clean, zip rebuilt 74 files v0.38.1, P0 fixes verified present in zip. NOT-bugs noted (left as-is): redundant Cache::purge after DB::delete, dead '2'/'0' in status skip array (defensive), no dedup on stacked delete/untrash pendings (harmless no-op), stale "medical-society" comment in seo-tools.

**STATUS — GBP integration SHIPPED 2026-06-10 (v0.38.0), zip rebuilt (74 files):** New includes/class-gbp.php (CC_Assistant_GBP) mirrors GSC OAuth exactly: BYO OAuth client, scope business.manage, AES-GCM token storage (key suffix |cc-gbp), admin_init callback handler (cc_gbp_oauth in cc-assistant.php), redirect-URI override for local sites. Methods: list_accounts (mybusinessaccountmanagement), list_locations w/ readMask (mybusinessbusinessinformation, categories+address+phone+website), fetch_locations (caches to cc_assistant_gbp_locations option), fetch_performance (businessprofileperformance fetchMultiDailyMetricsTimeSeries, calls/website/directions/impressions, 3-day lag). Settings → Business Profile tab (settings.php: valid_tabs+save+msg+nav+panel) with connect/disconnect/refresh-locations + locations table. MCP tools gbp_locations + gbp_performance → REST /gbp/locations + /gbp/performance (read-only, no pending). REVIEWS deliberately excluded (need Google legacy Business Profile API v4 access approval — class structured to add later). USER ACTION NEEDED to use it: enable 3 APIs (Account Mgmt + Business Information + Performance) in Cloud, create ONE OAuth client (Web app) w/ redirect URI shown in the panel, paste Client ID+Secret in Settings→Business Profile, Connect. NOT keys — OAuth. plugintesting offline so untested live; mirrors proven GSC flow. All php -l clean.

**STATUS — Release B / "global de-hardcoding + Tier 0" SHIPPED 2026-06-10 (v0.37.0), zip rebuilt at app/public/wp-content/plugins/cc-assistant.zip:** Driven by user directive "fix everything + make it global not hardcoded (4 sites + more coming)". Done: (1) class-similarity.php:351 hardcoded irving/tx/texas → per-site title tokens + cc_assistant_geo_terms option (THE correctness bug, fixes clustering on all non-Irving sites); (2) class-seo-tools.php medical auth_hosts (2 copies) → single industry-aware authority_hosts() helper (.gov/.edu/who.int universal + healthcare/financial_services/legal_practice/home_services overlays via Industry_Profile::industry_slug() + cc_assistant_authority_hosts option); (3) byline regex (2 copies) → Unicode \p{Lu}/\p{L} + Spanish (por/escrito por/revisado por); (4) FAQ-title normalizer fillers de-domained; (5) class-diff-render.php: added create/delete/untrash_redirect diff cards + generic key/value fallback (fixes the v0.36 Release-A gap where redirect tools + ~8 change types showed "no preview"); (6) class-schema-cleanup.php log_cleanup_throttled → no-op (removes FE-render DB write + blank-log bug); (7) get_post auto-slim guard (body+tree>60KB auto-slims, filterable cc_assistant_get_post_auto_slim_bytes, explicit slim=false honored). All 7 files php -l clean; version synced 0.37.0 (header/constant/readme/MCP serverInfo). REST-layer fixes live on plugintesting immediately; MCP serverInfo needs server restart; other 3 sites need zip upload.

NOT YET DONE (remaining waves, user wants all): T1 continuity cc_working_state subsystem; T1 analytics DEEP refactor (language segmentation in missing_mentions/content_gaps, unify the 2 pre-AIO CTR curves, confidence flags, classify_cluster hub-spoke guard); T2 page-quality (absolute image gate, IA/grouping check, placeholder/dup-card lint, build-from-spec template macro); T2 tool-surface consolidation (rename draft_update_post_meta→post_field w/ alias, merge ~25 audit/score tools, gsc_* family, unify post_id, pagination, trim descriptions); T3 NET-NEW features needing external creds/decisions: reviews mgmt + GBP/local-pack (need Google Business Profile API OAuth from user), citation/NAP tracker, AEO authoring, image-sourcing closed loop, admin "what is AI doing" activity view. Deferred tool rename from this wave (consolidation work, do in T2 with aliases).

**LIVE SCHEMA-EMITTER BUGS — found on erofirving 2026-06-24 (v0.45.1), verified via render_probe/managed_schema (NOT yet fixed in code):** (1) `propose_schema` APPLY path REGENERATES page-jsonld at apply time instead of writing the reviewed `proposed_value` verbatim — proven on post 3886: approving the regenerate pending updated the logo URL but left the FAQ answer text broken. (2) The generator FAILS to JSON-escape inner double-quotes inside FAQ answer text AND collapses `\n`/`\t` escapes (stored as literal `nn`/`n t`) → invalid JSON (`valid:false`, suppressed at render so no live error, but dead DB residue). 3886's "time is brain." quote triggered it; the 5 other page-jsonld posts are valid ONLY because their FAQ answers contain no inner quotes — latent landmine for any future FAQ with a quote/colon-list. Fix = proper json_encode of FAQ text + make apply write the reviewed payload, not regenerate. (3) page-jsonld emitter duplicates Rank Math on ALL 6 carrier posts (3201/3324/3207/3360/3332/3886): its Article ≈ Rank Math BlogPosting and its BreadcrumbList is an outright duplicate → render_probe `duplicate_breadcrumb` warn; only FAQPage is net-new. ENHANCEMENT = emit FAQPage-only when an SEO plugin already provides Article+Breadcrumb (kills the warn site-wide). WORKAROUND APPLIED this session: cleared 3886's broken page-jsonld (pending 393) since Rank Math already covers it; FAQ schema can be regenerated after the escaping fix ships. Also removed cc-assistant's redundant homepage EmergencyService dup on 228 (pending 392, applied — homepage now 2 clean blocks). These are T0-correctness items. Relates to schema-cleanup track above.

**STATUS — Release A SHIPPED 2026-06-10 (v0.36.0), zip built at app/public/wp-content/plugins/cc-assistant.zip:** (1) dedup status filter — find_existing_redirect_for_source skips inactive/trashed rows; (2) draft_delete_redirect + draft_untrash_redirect (REST /draft/redirect/delete|untrash, apply_delete_redirect/apply_untrash_redirect via RankMath DB::delete/change_status('active')+Cache::purge, MCP tools+dispatch); (3) replace_section_content now lints (CC_Assistant_Sections::lint_widget_updates mirrors container_add) + override_lint wired REST+MCP; (4) tmp/ deleted; (5) version synced header+constant+readme(Stable 0.36.0, Tested 7.0)+MCP serverInfo 0.36.0. All 5 files php -l clean. NOTE: REST-layer fixes (dedup, section lint) live immediately on plugintesting; NEW MCP tools + serverInfo + section override_lint schema need MCP server RESTART to appear in a session. Other 3 sites need the zip uploaded. NOT yet done: Release B (continuity cc_working_state), Release C (page quality), and the dropped flush_all RM-purge (intentional). Dropped because dedup fix removes the ghost-row at source.
