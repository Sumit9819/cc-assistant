/* Visible keyboard focus (WCAG 2.4.7): navy ring plus a white halo so it shows on white, grey, red and blue backgrounds; includes iframes. */
a:focus-visible,
button:focus-visible,
input:focus-visible,
select:focus-visible,
textarea:focus-visible,
iframe:focus-visible,
[tabindex]:focus-visible {
  outline: 3px solid #041562 !important;
  outline-offset: 0;
  box-shadow: 0 0 0 6px #FFFFFF !important;
}

/* Body-text links: brand Secondary blue, underlined (WCAG 1.4.3 + 1.4.1). Scoped to text widgets so buttons and menus keep their own styling. */
.elementor-widget-text-editor a,
.elementor-widget-theme-post-content a,
.elementor-icon-box-description a,
.page-content a,
.entry-content a {
  color: #11468F;
  text-decoration: underline;
  text-underline-offset: 2px;
}

/* Linked heading widgets (footer legal row) keep the heading colour on classic pages too, where Elementor's own heading CSS is not loaded. */
.elementor-heading-title a {
  color: inherit;
}

/* In-body cards: reset the browser default 40px <figure> margin. Content width
   on desktop, both screen edges on mobile. Not 100vw: the mobile container has
   10px left padding and 0 right, so the pull is asymmetric on purpose. */
.cc-card{margin:32px 0;width:100%}
.cc-card img{display:block;width:100%;height:auto}
@media (max-width:767px){.cc-card{margin-left:-10px;width:calc(100% + 10px)}}

/* Elementor inline icon lists outdent with negative side margins and hang
   outside their container: the post byline row sat 15px past the viewport and
   made every single-post page scroll sideways. !important because the margins
   come from per-widget CSS in template 3944, which the plugin cannot edit. */
.elementor-inline-items{margin-left:0!important;margin-right:0!important}
.elementor-inline-items>li:first-child{margin-left:0!important}
.elementor-inline-items>li:last-child{margin-right:0!important}